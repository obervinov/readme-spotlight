---
name: PR

on:
  push:
    branches: ['*', '*/*', '**', '!main']

jobs:
  pr:
    uses: obervinov/_templates/.github/workflows/pr.yaml@v2.2.2

  golang:
    uses: obervinov/_templates/.github/workflows/golang.yaml@v2.2.2

  build-pr-image:
    uses: obervinov/_templates/.github/workflows/docker.yaml@v2.2.2
    needs: [pr, golang]
