---
name: Release

on:
  pull_request:
    branches: [main]
    types: [closed]

# Scoped for the reusable workflows: create the release/tag and GHCR push.
permissions:
  contents: write
  packages: write
  pull-requests: write
  security-events: write
  actions: write
  checks: read

jobs:
  create-release:
    if: github.event.pull_request.merged == true
    uses: obervinov/_templates/.github/workflows/release.yaml@v4.0.0

  build-release-image:
    if: github.event.pull_request.merged == true
    uses: obervinov/_templates/.github/workflows/docker.yaml@v4.0.0
    needs: [create-release]
